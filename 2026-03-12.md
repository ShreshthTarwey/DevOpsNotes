# Docker Practical Commands Analysis - March 12, 2026

This document analyzes the Docker commands executed in the class practical session, explaining what each command does and key insights from their outputs.

## 1. `docker version`

**Command:** `docker version`

**Purpose:** Displays version information for both the Docker client and server (daemon).

**Analysis:**
- **Client Information:**
  - Version: 29.2.1
  - API Version: 1.53
  - Go Version: go1.25.6
  - Git Commit: a5c7197
  - Built: Mon Feb 2 17:20:16 2026
  - OS/Arch: windows/amd64
  - Context: desktop-linux

- **Server Information:**
  - Docker Desktop Version: 4.63.0 (220185)
  - Engine Version: 29.2.1
  - API Version: 1.53 (minimum version 1.44)
  - Go Version: go1.25.6
  - Git Commit: 6bc6209
  - Built: Mon Feb 2 17:17:24 2026
  - OS/Arch: linux/amd64
  - Experimental: false

**Key Insights:**
- Client runs on Windows (windows/amd64), server runs on Linux (linux/amd64) via Docker Desktop
- API versions are compatible (client 1.53, server minimum 1.44)
- Built dates indicate recent versions from February 2026
- Context "desktop-linux" shows Docker Desktop is configured for Linux containers

## 2. `docker --version`

**Command:** `docker --version`

**Purpose:** Shows a concise version string for the Docker client.

**Analysis:**
- Output: "Docker version 29.2.1, build a5c7197"

**Key Insights:**
- Simplified version info compared to `docker version`
- Useful for quick version checks in scripts

## 3. `docker info`

**Command:** `docker info`

**Purpose:** Displays system-wide information about Docker installation and configuration.

**Analysis:**
- **Client Details:**
  - Version: 29.2.1
  - Context: desktop-linux
  - Debug Mode: false

- **Plugins:** Multiple Docker plugins installed including:
  - agent, ai, buildx, compose, debug, desktop, extension, init, mcp, model, offload, pass, sandbox, sbom, scout

- **Server Details:**
  - Containers: 1 total (0 running, 0 paused, 1 stopped)
  - Images: 1
  - Server Version: 29.2.1
  - Storage Driver: overlayfs
  - Logging Driver: json-file
  - Cgroup Driver: cgroupfs (Version 2)
  - Plugins: Volume (local), Network (bridge, host, ipvlan, macvlan, null, overlay), Log drivers
  - Runtimes: io.containerd.runc.v2, nvidia, runc
  - Default Runtime: runc
  - Security Options: seccomp (builtin profile), cgroupns
  - Kernel Version: 6.6.87.2-microsoft-standard-WSL2
  - Operating System: Docker Desktop
  - OSType: linux
  - Architecture: x86_64
  - CPUs: 16
  - Total Memory: 7.467GiB
  - Docker Root Dir: /var/lib/docker
  - HTTP/HTTPS Proxy configured for docker.internal
  - Live Restore: Disabled
  - Firewall Backend: iptables

**Key Insights:**
- Docker Desktop running on WSL2 backend
- 16 CPUs and ~7.5GB RAM available
- One container exists but is stopped
- Multiple plugins indicate full Docker Desktop installation
- NVIDIA runtime available (GPU support)
- Proxy configuration for corporate environments

## 4. `docker history` (Error Case)

**Command:** `docker history`

**Purpose:** Shows the history of an image (layers and build steps).

**Analysis:**
- Error: "docker: 'docker history' requires 1 argument"
- Usage message displayed

**Key Insights:**
- Command requires an image name/tag as argument
- Demonstrates proper error handling and usage guidance

## 5. `docker history hello-world`

**Command:** `docker history hello-world`

**Purpose:** Shows the build history/layers of the hello-world image.

**Analysis:**
- IMAGE: ef54e839ef54
- CREATED: 7 months ago
- Layers:
  - CMD ["/hello"] - 0B (entrypoint command)
  - COPY hello / - 16.4kB (copying hello binary)

**Key Insights:**
- hello-world is a minimal image with just two layers
- Total size: ~16.4kB
- Built using BuildKit (buildkit.dockerfile.v0)

## 6. `docker pull httpd`

**Command:** `docker pull httpd`

**Purpose:** Downloads the latest httpd (Apache HTTP Server) image from Docker Hub.

**Analysis:**
- Pulled 8 layers: 206356c42440, 242bb693a1d3, 47a6bbb8d6d8, 7079f4501db6, 80414dfb8f15, 4f4fb700ef54, ea9cd2f459bd, 44fabeaa234c
- Digest: sha256:96b1e8f69ee3adde956e819f7a7c3e706edef7ad88a26a491734015e5c595333
- Status: Downloaded newer image for httpd:latest

**Key Insights:**
- Successfully pulled official httpd image
- Image digest provides verification hash
- Multiple layers indicate complex build process

## 7. `docker history hrrpd` (Error Case)

**Command:** `docker history hrrpd`

**Purpose:** Attempted to show history of non-existent image.

**Analysis:**
- Error: "Error response from daemon: No such image: hrrpd:latest"

**Key Insights:**
- Demonstrates error when image doesn't exist locally
- Docker checks local images first before suggesting pull

## 8. `docker history httpd`

**Command:** `docker history httpd`

**Purpose:** Shows the build history of the httpd image.

**Analysis:**
- Multiple layers showing the build process:
  - CMD ["httpd-foreground"] - 0B
  - EXPOSE map[80/tcp:{}] - 0B
  - COPY httpd-foreground script - 20.5kB
  - STOPSIGNAL SIGWINCH - 0B
  - RUN package installation - 34.9MB
  - ENV variables for HTTPD version/patches - 0B
  - RUN source download and compilation - 7MB
  - WORKDIR /usr/local/apache2 - 4.1kB
  - RUN directory creation - 16.4kB
  - ENV PATH settings - 0B
  - Base image layer - 87.4MB

**Key Insights:**
- Complex multi-stage build process
- Total image size: ~47.6MB (from docker images output)
- Built 2 weeks ago using BuildKit
- Shows typical Dockerfile instructions (RUN, COPY, ENV, etc.)

## 9. `docker images`

**Command:** `docker images`

**Purpose:** Lists all Docker images on the system.

**Analysis:**
- hello-world:latest - ID: ef54e839ef54, Disk Usage: 25.9kB, Content Size: 9.52kB
- httpd:latest - ID: 96b1e8f69ee3, Disk Usage: 177MB, Content Size: 47.6MB

**Key Insights:**
- Two images present: minimal hello-world and larger httpd
- Disk usage includes metadata, content size is actual image size
- "U" indicates images are in use (referenced by containers)

## 10. `docker image ls`

**Command:** `docker image ls`

**Purpose:** Alternative command to list Docker images (same as `docker images`).

**Analysis:**
- Same output as `docker images`

**Key Insights:**
- `docker image ls` is the newer recommended syntax
- `docker images` is legacy but still works

## 11. `docker run httpd echo "Hello World"`

**Command:** `docker run httpd echo "Hello World"`

**Purpose:** Runs a container from httpd image, executes echo command, then exits.

**Analysis:**
- Output: "Hello World"
- Container runs and exits immediately

**Key Insights:**
- httpd image can run arbitrary commands
- Container lifecycle: create → run → exit
- No persistent container remains

## 12. `docker run --name lpu httpd echo "vjndv"`

**Command:** `docker run --name lpu httpd echo "vjndv"`

**Purpose:** Runs a container with custom name "lpu", executes echo command.

**Analysis:**
- Output: "vjndv"
- Container named "lpu" created and ran

**Key Insights:**
- `--name` flag assigns custom container name
- Useful for container identification and management
- Container still exits after command completion

## Summary of Key Docker Concepts Demonstrated

1. **Version Management:** Checking client/server versions and compatibility
2. **System Information:** Understanding Docker installation and host capabilities
3. **Image Management:** Pulling, listing, and inspecting images
4. **Image History:** Understanding image layers and build process
5. **Container Execution:** Running containers with and without custom names
6. **Error Handling:** Proper error messages for invalid commands/images
7. **Docker Desktop:** WSL2 backend, plugins, and resource allocation

## Practical Takeaways

- Docker client runs on Windows, server on Linux via Docker Desktop
- Images consist of layers showing build history
- Containers are ephemeral by default
- Proper naming helps with container management
- Error messages provide clear guidance for troubleshooting